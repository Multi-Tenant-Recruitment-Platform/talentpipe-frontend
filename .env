VITE_API_BASE_URL=/api/v1

# Apex domain that workspaces live under. Used to read the tenant out of the
# hostname (acme.talentpipe.io → 'acme') and to render the workspace suffix.
VITE_APP_ROOT_DOMAIN=talentpipe.io
