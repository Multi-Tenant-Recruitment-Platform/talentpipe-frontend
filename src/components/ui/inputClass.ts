/**
 * The one text-input style in the app. Was copy-pasted verbatim across five
 * forms; kept here so a change to the focus ring lands everywhere at once.
 */
export const inputClass =
  'mt-1 block w-full rounded-md border border-slate-300 px-3 py-2 text-sm shadow-sm ' +
  'focus:border-indigo-500 focus:outline-none focus:ring-1 focus:ring-indigo-500 ' +
  'disabled:cursor-not-allowed disabled:bg-slate-50 disabled:text-slate-500';
